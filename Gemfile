# A sample Gemfile
source "http://rubygems.org"

gem 'foreman'

group :tracker do
  gem 'eventmachine'
  gem 'em-hiredis'
  gem 'eventmachine_httpserver'
  gem 'rack' # just for request parsing. I'm lazy.
end
